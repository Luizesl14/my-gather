# PENDING — ATV-0501 — Criar schema Workspace

## Entregue parcialmente

- `backend/prisma/schema.prisma` modela:
  - `workspaces`
  - `floors`
  - `maps`
  - `rooms`
  - `desks`
- Migration criada em `backend/prisma/migrations/20260604180000_workspace_schema/migration.sql`.
- Relações declaradas:
  - workspace pertence à organização.
  - floor pertence ao workspace.
  - map pertence ao floor.
  - rooms/desks pertencem ao workspace e ao floor.
  - desk pode ser atribuído a usuário.

## Pendente para `done`

- Executar a migration contra PostgreSQL local.

## Bloqueio

Docker daemon não está acessível nesta máquina:

- `dial unix /home/luizaraujo/.docker/desktop/docker.sock: connect: no such file or directory`

## Validação realizada

- `DATABASE_URL=... npx prisma validate` passou.
- `npm run db:generate` passou.
- `npm run build` passou.
- `npm test` passou.

## Referência

- `.specs/00-implementation-sequence-spec.md` (ATV-0501)
- `.specs/done/05-workspace/23-workspace-domain-spec.md`
