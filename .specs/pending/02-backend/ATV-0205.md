# PENDING — ATV-0205 — Criar Prisma

## Entregue parcialmente

- Prisma configurado com schema em `backend/prisma/schema.prisma`
- Migration inicial criada em `backend/prisma/migrations/20260604170000_init/migration.sql`
- Prisma Client gerado e disponível via `backend/src/shared/infrastructure/database/prisma-client.ts`
- Scripts adicionados em `backend/package.json`:
  - `db:generate`
  - `db:migrate:dev`
  - `db:migrate:deploy`

## Pendente para voltar a `done`

- Executar a migration inicial contra PostgreSQL local.
- Confirmar conexão usando `DATABASE_URL`.

O Docker daemon não estava acessível nesta máquina:

- `dial unix /home/luizaraujo/.docker/desktop/docker.sock: connect: no such file or directory`

Assim que o Docker/PostgreSQL estiver disponível, rodar:

- `docker compose up -d postgres redis`
- `cd backend && DATABASE_URL=... npm run db:migrate:dev`

## Referência

- `.specs/00-implementation-sequence-spec.md` (ATV-0205)
- `.specs/todo/20-database-orm-spec.md`
