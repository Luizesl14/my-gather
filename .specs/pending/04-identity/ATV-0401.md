# PENDING — ATV-0401 — Criar schema inicial Identity

## Entregue parcialmente

- `backend/prisma/schema.prisma` modela:
  - `users`
  - `organizations`
  - `memberships`
  - `invitations`
- Migration criada em `backend/prisma/migrations/20260604172000_identity_schema/migration.sql`.
- Constraints e relações declaradas:
  - email único em `users`
  - token único em `invitations`
  - membership único por `userId` + `organizationId`
  - FKs entre usuário, organização, membership e invitation

## Pendente para `done`

- Executar a migration contra PostgreSQL local.

## Bloqueio

Docker daemon não está acessível nesta máquina:

- `dial unix /home/luizaraujo/.docker/desktop/docker.sock: connect: no such file or directory`

## Validação realizada

- `DATABASE_URL=... npx prisma validate` passou.
- `npm run db:generate` passou.
- `npm run build` passou.
- `npm test` passou.

## Referência

- `.specs/00-implementation-sequence-spec.md` (ATV-0401)
