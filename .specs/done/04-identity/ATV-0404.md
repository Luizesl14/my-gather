# DONE — ATV-0404 — Criar REST Identity

## Entregue

- Rotas HTTP:
  - `POST /auth/register`
  - `POST /auth/login`
  - `POST /auth/logout`
  - `GET /auth/me`
  - `POST /organizations`
  - `GET /organizations`
  - `GET /organizations/:id`
  - `POST /organizations/:id/invitations`
  - `POST /invitations/:token/accept`
- Schemas Zod em `backend/src/modules/identity/presentation/schemas/identity-schemas.ts`.
- Mappers DTO evitando retorno de senha/hash.
- Token JWT HS256 básico no login.
- Repositório Identity em memória para permitir integração sem banco nesta fase.
- Erros em formato consistente `{ error: { code, message } }`.

## Validação

- Testes de integração cobrem register/login/auth me/erros.
- `npm run build` passou.
- `npm test` passou.

## Referência

- `.specs/00-implementation-sequence-spec.md` (ATV-0404)
- `.specs/done/04-identity/22-identity-api-spec.md`
