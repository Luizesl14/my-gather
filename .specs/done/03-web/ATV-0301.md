# DONE — ATV-0301 — Criar projeto Flutter Web

## Entregue

- `web/pubspec.yaml`, `web/lib/main.dart` e `web/lib/app.dart` criados.
- `web/test/` e `web/assets/` presentes.
- Dependências configuradas: Riverpod, GoRouter, Dio, WebSocket client e Flame.
- Tela inicial renderiza `Love+Robot`.

## Validação

- `flutter test` passou.
- `flutter build web` passou.

## Observação

- `flutter run -d chrome` não foi usado para manter a execução não interativa, mas o build web validou a compilação para navegador.

## Referência

- `.specs/00-implementation-sequence-spec.md` (ATV-0301)
- `.specs/done/03-web/16-web-package-spec.md`
