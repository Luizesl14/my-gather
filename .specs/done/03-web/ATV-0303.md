# DONE — ATV-0303 — Criar design system Flutter light/dark

## Entregue

- Tokens de cor em `web/lib/core/theme/app_colors.dart`.
- Tokens de espaçamento em `web/lib/core/theme/app_spacing.dart`.
- Tokens de raio em `web/lib/core/theme/app_radius.dart`.
- Tipografia base em `web/lib/core/theme/app_typography.dart`.
- Tema light/dark em `web/lib/core/theme/app_theme.dart`.
- `MaterialApp` usa `AppTheme.light`, `AppTheme.dark` e `ThemeMode.system`.
- Componentes base criados em `web/lib/shared/design_system/`:
  - `AppIconButton`
  - `AppTooltip`
  - `AppPanel`
  - `AppModal`
  - `AppBadge`
  - `AppStatusDot`

## Validação

- `dart format lib test` passou.
- `flutter analyze` passou.
- `flutter test` passou.
- `flutter build web` passou.

## Referência

- `.specs/00-implementation-sequence-spec.md` (ATV-0303)
- `.specs/done/03-web/31-web-theme-design-system-spec.md`
- `.specs/context/02-theme-layout-spec.md`
