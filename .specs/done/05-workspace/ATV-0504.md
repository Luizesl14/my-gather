# DONE — ATV-0504 — Criar endpoints de workspace/mapa

## Entregue

- `POST /organizations/:organizationId/workspaces`
- `GET /organizations/:organizationId/workspaces`
- `GET /workspaces/:workspaceId`
- `GET /workspaces/:workspaceId/map`
- Repositório Workspace em memória.
- Loader do mapa padrão `web/assets/maps/office-default.json`.
- Endpoint de mapa retorna:
  - `workspace`
  - `floor`
  - `map`
  - `layers`
  - `collision`
  - `rooms`
  - `desks`
  - `interactiveZones`
  - `assetPackId`

## Validação

- Testes de integração cobrem criação/listagem de workspace.
- Testes de integração cobrem payload de mapa renderizável.
- `npm run build` passou.
- `npm test` passou.

## Observação

- Persistência ainda está em memória porque as migrations seguem bloqueadas por Docker/PostgreSQL local indisponível.

## Referência

- `.specs/00-implementation-sequence-spec.md` (ATV-0504)
- `.specs/done/05-workspace/24-workspace-api-map-spec.md`
