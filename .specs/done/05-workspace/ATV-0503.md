# DONE — ATV-0503 — Criar mapa mockado inicial

## Entregue

- Mapa renderizável criado em `web/assets/maps/office-default.json`.
- JSON contém `id`, `version`, `width`, `height`, `tileSize`, `assetPackId`, `layers`, `collision` e `interactiveZones`.
- Assets de tiles e móveis existem em `web/assets/tilesets/` e `web/assets/furniture/`.
- `web/pubspec.yaml` registra `assets/`.

## Validação

- `jq` validou estrutura mínima do mapa.
- Mapa possui 4 layers, 8 colisões, 3 zonas interativas, 1 sala e 2 mesas.

## Referência

- `.specs/00-implementation-sequence-spec.md` (ATV-0503)
- `.specs/context/06-map-assets-avatar-spec.md`
